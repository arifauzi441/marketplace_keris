name: Build Flutter Mobile Admin

on:
  push:
    branches:
      - main
    paths:
      - 'mobile_admin/**'
  pull_request:
    branches:
      - main
    paths:
      - 'mobile_admin/**'

permissions:
  contents: write

jobs:
  build-flutter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64_ADMIN }}" | base64 -d > mobile_admin/android/app/my-release-key2.jks

      - name: Create key.properties
        run: |
          cat <<EOF > mobile_admin/android/key.properties
          storePassword=${{ secrets.KEYSTORE_PASSWORD_ADMIN }}
          keyPassword=${{ secrets.KEY_PASSWORD_ADMIN }}
          keyAlias=${{ secrets.KEY_ALIAS_ADMIN }}
          storeFile=../app/my-release-key2.jks
          EOF

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'

      - name: Install dependencies
        run: flutter pub get
        working-directory: mobile_admin

      - name: Build APK release
        run: flutter build apk --release
        working-directory: mobile_admin

      - name: List APK folder
        run: ls -la mobile_admin/build/app/outputs/flutter-apk/

      - name: Rename APK
        run: mv mobile_admin/build/app/outputs/flutter-apk/app-release.apk mobile_admin/build/app/outputs/flutter-apk/toko-keris-admin.apk

      - name: Copy APK to backend/public/apk
        run: |
          mkdir -p backend/public/apk
          cp mobile_admin/build/app/outputs/flutter-apk/toko-keris-admin.apk backend/public/apk/

      - name: Commit and push APK to repo
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git add backend/public/apk/toko-keris-admin.apk
          git commit -m "Update APK pengguna"
          for i in 1 2 3; do
          git push && break || {
            echo "Push failed, retrying in 5s..."
            sleep 5
            git pull origin main --rebase || true
          }
          done

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: flutter-apk-keris-admin
          path: mobile_admin/build/app/outputs/flutter-apk/toko-keris-admin.apk

      - name: Build AAB release
        run: flutter build appbundle --release
        working-directory: mobile_admin

      - name: List AAB folder
        run: ls -la mobile_admin/build/app/outputs/bundle/release/

      - name: Upload AAB as artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-aab-keris-admin
          path: mobile_admin/build/app/outputs/bundle/release/app-release.aab